###Backend development
1.create git repo
    git init        
2. add .gitignore file
3.create .env file for environment variables 
  & read data from .env:-
  with "dotenv" module  npm i dotenv

4.Generate package.json  
    npm init -y
5. Create express app
    npm i express
6.Connect to DB
    npm i mongoose
7.Add middlewares (body parser middleware, error handling middlewares)

8.Design schemas and create models

9. Design REST APIs for all resources 
10.Registra.n and login in common for USER and AUTHOR. Create a separate service to reuse
11.The client wont send role.Ti just redirects to a specific API ..